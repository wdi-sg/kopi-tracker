<h1>order index</h1>
<% if user_signed_in? %>
<%= link_to 'New Order', new_order_path %>
<% end %>

<ul>
	<%= @orders.each do |order| %>
		<li>
			<p><%= order.weight %></p>
		</li>
		<%= link_to 'Show', order_path(order) %>
	<% end %>
</ul>