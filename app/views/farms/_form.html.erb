<%= form_with model: @farm, local: true do |form| %>

	<% if @farm.errors.any? %>
		<div id="error_explanation">
			<h2>
				<%= pluralize(@farm.errors.count, "error") %> prohibited
				this farm from being saved:
			</h2>
			<ul>
				<% @farm.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<p>
		<%= form.label :location %><br>
		<%= form.text_field :location %>
	</p>

	<p>
		<%= form.label :phone %><br>
		<%= form.text_area :phone %>
	</p>

	<p>
		<%= form.submit %>
	</p>

<% end %>
