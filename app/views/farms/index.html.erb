<h1>Listing farms</h1>

<table>
  <tr>
    <th>Locations</th>
    <th>Phone</th>
    <th></th>
  </tr>

  <% @farms.each do |farm| %>
    <tr>
      <td><%= farm.location %></td>
      <td><%= farm.phone %></td>
      <td><%= link_to 'Show', farm_path(farm) %></td>
    </tr>
  <% end %>
</table>