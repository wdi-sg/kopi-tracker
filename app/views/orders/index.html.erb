<h1>Listing orders</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Weight</th>
    <th>Coffee</th>
    <th>Price(/kg)</th>
    <th>Total Cost</th>
    <th></th>
  </tr>

  <% @orders.each do |order| %>
    <tr>
      <td><%= order.id %></td>
      <td><%= order.weight %>kg</td>
      <td><%= order.coffee.name %></td>
      <td>$<%= order.coffee.price %></td>
      <td>$<%= order.weight*order.coffee.price %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New order', new_order_path %>