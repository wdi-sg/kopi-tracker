<%= form_with scope: :order, url: orders_path, local: true do |form| %>
  <p>
    <%= form.label :weight %><br>
    <%= form.text_field :weight %>
  </p>

  <%= form.collection_select :coffee_id, @coffees, :id, :name_with_price %>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= form_with scope: :order, url: orders_path, local: true do |form| %>
  ...
<% end %>

<%= link_to 'Back', root_path %>

<h1>Menu</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Roast</th>
    <th>Origin</th>
    <th>Price($/kg)</th>
    <th></th>
  </tr>

  <% @coffees.each do |coffee| %>
    <tr>
      <td><%= coffee.name %></td>
      <td><%= coffee.roast %></td>
      <td><%= coffee.origin.name %></td>
      <td><%= coffee.price %></td>
    </tr>
  <% end %>
</table>