
<% if user_signed_in? %>

<div class="container-fluid">
	<div class="row">
		<div class="col-lg-3">
			<%= render 'navside'  %>
		</div>
		<div class="col-lg">
			<%= render 'content' %>
		</div>
	</div>
</div>

<% else %>

<div class="container-fluid">
	<div class="row">
		<div class="col-lg">
			<%= render 'content' %>
		</div>
	</div>
</div>

<% end %>