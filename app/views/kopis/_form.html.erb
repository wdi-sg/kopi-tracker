<%= form_with(model: kopi, local: true) do |form| %>
  <%= form.label :name %><br />
  <%= form.text_field :name %><br />
  <%= form.label :roast_id %><br />
  <%= form.collection_select :roast_id, @roasts, :id, :display_roast %><br />
  <%= form.label :origin_id %><br />
  <%= form.collection_select :origin_id, @origins, :id, :location %><br />

  <% if kopi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kopi.errors.count, "error") %> prohibited this kopi from being saved:</h2>

      <ul>
      <% kopi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
