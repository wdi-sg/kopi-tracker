
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Kopi Tracker</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

<% if user_signed_in? %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Add a farm', new_origin_path %><span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Match Kopi To Farm', new_kopi_path %></a>
      </li>
        <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Customers', customers_path %></a>
      </li>
       </li>
        <li class="nav-item active logout">
        <a class="nav-link" href="#"><%= link_to 'Logout', destroy_user_session_url, method: :delete %></a>
      </li>
    </ul>
  </div>

  <% else %>

 <ul class="navbar-nav mr-auto">
	<li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Sign In', new_user_session_path %></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Sign Up', new_user_registration_path %></a>
      </li>
    </ul>
	<% end %>
</nav>




<% if user_signed_in? %>
<div class="kopi-farm-container">

<% @kopis.each do |kopi| %>
<div class="kopi-farm container">
		<p class="kopi-name" style="font-weight: bold; font-size: 20px">Kopi Name:</p>
		<%= kopi.name %>
		<p style="font-weight: bold; font-size: 20px">Kopi Roast: </p><%= kopi.roast %>
		<p><%= link_to 'Show', kopi_path(kopi) %></p>
		<p><%= link_to 'Edit', edit_kopi_path(kopi) %></p>
		<p><%= link_to 'Delete', kopi_path(kopi), method: :delete, data:{ confirm: 'Are you sure?'} %></p>
</div>
</div>
<% end %>
<% else %>
<div class="kopi-farm-container">
<div class="kopi-farm container">
<% @kopis.each do |kopi| %>


	<p class="kopi-name" style="font-weight: bold; font-size: 20px">Kopi Name:</p>
		<%= kopi.name %>
		<p style="font-weight: bold; font-size: 20px">Kopi Roast: </p><%= kopi.roast %>
</div>
</div>
		<% end %>
<% end %>




<div class="kopi-farm-container">
<% if user_signed_in? %>

<div class="kopi-farm container">

<% @origins.each do |origin| %>


<p style="font-weight: bold; font-size: 20px">Location:</p> <%= origin.location %>
<p style="font-weight: bold; font-size: 20px">Phone Number: </p><%= origin.phone_no %>
<p><%= link_to 'Show', origin_path(origin) %></p>

<p><%= link_to 'Edit', edit_origin_path(origin) %></p>
<p><%= link_to 'Delete', origin_path(origin), method: :delete %></p>
</div>
</div>

<% end %>

<% else %>
<div class="kopi-farm-container">
<div class="kopi-farm container">

<% @origins.each do |origin| %>


<p style="font-weight: bold; font-size: 20px">Location:</p> <%= origin.location %>
<p style="font-weight: bold; font-size: 20px">Phone Number: </p><%= origin.phone_no %>
</div>

<% end %>
<% end %>
</div>