<h2>Origins</h2>


<% @origins.each do |origin| %>
  <p><%=link_to origin.name, origin_path(origin) %></p>
<% end %>

<% if current_user.admin?  %>
  <%= link_to "add new origin", new_origin_path %>
<% end %>