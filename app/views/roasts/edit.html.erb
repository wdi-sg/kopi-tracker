<h1>Edit Roast</h1>

<%= form_with(model: @roast, local: true) do |form| %>

  <% if @roast.errors.any? %>
	<div id="error_explanation">
	  <h2>
		<%= pluralize(@roast.errors.count, "error") %> prohibited
		this roast from being saved:
	  </h2>
	  <ul>
		<% @roast.errors.full_messages.each do |msg| %>
		  <li><%= msg %></li>
		<% end %>
	  </ul>
	</div>
  <% end %>

  <p>
	<%= form.label :name %><br>
	<%= form.text_field :name %>
  </p>

  <p>
	<%= form.submit %>
  </p>

<% end %>

<%= link_to 'Back', root_path %>