<%= form_with(model: customers_kopi, local: true) do |form| %>
  <% if customers_kopi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customers_kopi.errors.count, "error") %> prohibited this customers_kopi from being saved:</h2>

      <ul>
      <% customers_kopi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="field">
    <%= form.label :kopi_id %>
    <%= form.text_field :kopi_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
