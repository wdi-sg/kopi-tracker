<h1>New Customer</h1>

<%= form_with scope: :customer, url: customers_path, local: true do |form| %>
    <p>
        <%= form.label :name %>
        <%= form.text_field :name %>
    </p>

    <p>
        <%= form.label :kopis %> <br />
        <%= form.collection_check_boxes :kopi_ids, @kopis, :id, :name %>
    </p>

    <p class="error">
    <%= show_errors(@customer, :name) %>
  </p>

    <p>
        <%= form.submit %>
    </p>
<% end %>

<%= link_to 'Back', customers_path %>
