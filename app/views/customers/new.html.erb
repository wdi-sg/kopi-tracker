<%= form_with scope: :customer, url: customers_path, local: true do |form| %>

  <%= form.collection_check_boxes :coffee_ids, @coffees, :id, :name %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_area :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>