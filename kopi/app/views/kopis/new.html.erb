<%= form_with scope: :kopi, url: kopis_path, local: true do |form| %>

	<%= form.collection_select :roast_id, @roasts, :id, :name %><br>
	<%= form.collection_select :origin_id, @origins, :id, :name %>

	<p>
		<%= form.label :name %><br>
		<%= form.text_area :name %>
	</p>

	<p>
		<%= form.submit %>
	</p>

	<% end %>